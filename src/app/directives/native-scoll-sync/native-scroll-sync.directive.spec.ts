import { NativeScrollSyncDirective } from './native-scroll-sync.directive';

describe('NativeScrollSyncDirective', () => {
  it('should create an instance', () => {
    const directive = new NativeScrollSyncDirective();
    expect(directive).toBeTruthy();
  });
});
