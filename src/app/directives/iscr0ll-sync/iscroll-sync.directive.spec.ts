import { IscrollSyncDirective } from './iscroll-sync.directive';

describe('IscrollSyncDirective', () => {
  it('should create an instance', () => {
    const directive = new IscrollSyncDirective();
    expect(directive).toBeTruthy();
  });
});
