import { OverlayScrollSyncDirective } from './overlay-scroll-sync.directive';

describe('OverlayScrollSyncDirective', () => {
  it('should create an instance', () => {
    const directive = new OverlayScrollSyncDirective();
    expect(directive).toBeTruthy();
  });
});
