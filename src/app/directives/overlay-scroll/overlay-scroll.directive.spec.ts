import { OverlayScrollDirective } from './overlay-scroll.directive';

describe('OverlayScrollDirective', () => {
  it('should create an instance', () => {
    const directive = new OverlayScrollDirective();
    expect(directive).toBeTruthy();
  });
});
